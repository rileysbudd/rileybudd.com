<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Video Call</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: #1a1a1a;
            color: white;
            height: 100vh;
            overflow: hidden;
        }

        .video-container {
            position: relative;
            width: 100%;
            height: 100vh;
            background: #000;
        }

        .remote-video {
            width: 100%;
            height: 100%;
            background: #2a2a2a;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            color: #666;
        }

        .local-video {
            position: absolute;
            top: 20px;
            right: 20px;
            width: 150px;
            height: 200px;
            background: #3a3a3a;
            border-radius: 12px;
            border: 2px solid #4a4a4a;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.9rem;
            color: #666;
        }

        .call-info {
            position: absolute;
            top: 20px;
            left: 20px;
            background: rgba(0, 0, 0, 0.6);
            padding: 12px 20px;
            border-radius: 8px;
            backdrop-filter: blur(10px);
        }

        .call-info .name {
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 4px;
        }

        .call-info .duration {
            font-size: 0.9rem;
            color: #aaa;
        }

        .controls {
            position: absolute;
            bottom: 40px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            gap: 20px;
            background: rgba(0, 0, 0, 0.6);
            padding: 15px 30px;
            border-radius: 50px;
            backdrop-filter: blur(10px);
        }

        .control-btn {
            width: 56px;
            height: 56px;
            border-radius: 50%;
            border: none;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s;
            font-size: 1.3rem;
        }

        .control-btn:hover {
            transform: scale(1.1);
        }

        .control-btn.mute {
            background: #4a4a4a;
            color: white;
        }

        .control-btn.video {
            background: #4a4a4a;
            color: white;
        }

        .control-btn.end {
            background: #dc3545;
            color: white;
        }

        .control-btn.end:hover {
            background: #c82333;
        }

        /* Mobile adjustments */
        @media (max-width: 768px) {
            .local-video {
                width: 100px;
                height: 133px;
                top: 15px;
                right: 15px;
            }

            .call-info {
                top: 15px;
                left: 15px;
                padding: 10px 15px;
            }

            .call-info .name {
                font-size: 1rem;
            }

            .call-info .duration {
                font-size: 0.8rem;
            }

            .controls {
                bottom: 30px;
                gap: 15px;
                padding: 12px 20px;
            }

            .control-btn {
                width: 50px;
                height: 50px;
                font-size: 1.1rem;
            }
        }
    </style>
</head>
<body>
    <div class="video-container">
        <!-- Remote video placeholder -->
        <div class="remote-video">
            Remote Video
        </div>

        <!-- Local video placeholder -->
        <div class="local-video">
            You
        </div>

        <!-- Call info -->
        <div class="call-info">
            <div class="name">Contact Name</div>
            <div class="duration">00:00</div>
        </div>

        <!-- Control buttons -->
        <div class="controls">
            <button class="control-btn mute" aria-label="Mute microphone">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z"></path>
                    <path d="M19 10v2a7 7 0 0 1-14 0v-2"></path>
                    <line x1="12" y1="19" x2="12" y2="23"></line>
                    <line x1="8" y1="23" x2="16" y2="23"></line>
                </svg>
            </button>
            <button class="control-btn video" aria-label="Toggle video">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <polygon points="23 7 16 12 23 17 23 7"></polygon>
                    <rect x="1" y="5" width="15" height="14" rx="2" ry="2"></rect>
                </svg>
            </button>
            <button class="control-btn end" aria-label="End call">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
                </svg>
            </button>
        </div>
    </div>
</body>
</html>